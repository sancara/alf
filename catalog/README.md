# alf catalog

This directory is the seed catalog that `alf catalog init` copies to your machine
when you don't pass a `--remote`. It contains the seven built-in personas.

Your personal catalog lives at `~/.config/alf/catalog/` and is your own git repo.
Add skills, iterate with `alf plearn` / `alf glearn`, and push to your remote.
